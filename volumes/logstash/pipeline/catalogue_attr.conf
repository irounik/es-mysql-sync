input {
  jdbc {
    jdbc_driver_library => "/usr/share/logstash/mysql-connector-java-8.0.22.jar"
    jdbc_driver_class => "com.mysql.jdbc.Driver"
    jdbc_connection_string => "jdbc:mysql://services-in-db.saas.nextscm.com:3306/storejini"
    jdbc_user => "nextscm-app-user"
    jdbc_password => "nextscm@fashion"
    statement_filepath => "/usr/share/logstash/config/queries/catalogue_attr.sql"
    use_column_value => true
    tracking_column => "updated_at"
    tracking_column_type => "timestamp"
    schedule => "*/5 * * * * *"
  }
}

filter {
  mutate {
    add_field => { "%{attribute_code}" => "%{value}" }
    add_field => { "[@metadata][doc_id]" => "%{ssn}" }  
    remove_field => ["@version", "@timestamp", "ssn", "attribute_code", "value", "updated_at"]
  }
}

output {
  stdout {
    codec => json
  }
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    # user => "elastic"
    # password => "YigoXbi3xhgaTMJi0DC7CoXa"
    index => "catalogue"
    document_id => "%{[@metadata][doc_id]}"
    action => "update"
  }
}